# model class
model_type: null

# the checkpoint to load
load_ckpt: null
# the checkpoint path to save
save_ckpt: best
# use the debug mode (will train 2 steps and encode 10 steps)
debug: False

# load the encoded cache
load_encode: False
# load the existing retrieval result
load_result: False
# save the encoded result
save_encode: False
# save the model after main function
save_model: False
# save the retrieval result together with the score of each retrieved document
save_score: False

# return token_type_ids from PLM tokenizer?
return_type_id: False

# separate query and document encoder?
untie_encoder: false
