defaults:
  - _default

code_type: none
# the length of the code in the memmap file (leading 0)
code_length: 0
# the extra size of the code
code_size: 0
code_tokenizer: "t5"

# return pre-defined code for generative models
return_code: False
return_doct5: False

# the psudo query number per document
query_per_doc: 10
# the beams when decoding
nbeam: 10
